import { Box } from "@mui/material";
import React from "react";

const Summary = () => {
  return (
    <Box
      sx={{
        margin: 1,
        padding: 1,
        borderRadius: 5,
        backgroundColor: "#fff",
      }}
    >
      a
    </Box>
  );
};

export default Summary;
